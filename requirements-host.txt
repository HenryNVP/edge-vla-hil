# PC host (x86) — simulation plant + diffusion policy in PyTorch fallback mode.
# ROS2 Humble itself is installed via apt, not pip.
numpy>=1.24,<2.0
# robosuite 1.5.x has broken multi-camera offscreen rendering (obs images swap/corrupt after
# env.step — validated 2026-07-07); 1.4.1 is also the version family the diffusion_policy
# robomimic checkpoints were evaluated on (visuals match the training distribution).
robosuite==1.4.1
mujoco==2.3.7  # robosuite 1.4.x targets mujoco 2.3
# lerobot: pin PyPI release — git main fails pip metadata ("unknown" project name);
# 0.4+ moved normalization out of the policy (breaks from_pretrained for diffusion checkpoints).
torch>=2.2.1,<2.8.0
lerobot==0.3.3
opencv-python>=4.8
onnx>=1.15
onnxruntime>=1.17
pyyaml>=6.0
imageio[ffmpeg]>=2.31  # mp4 export in scripts/run_sim_standalone.py --video

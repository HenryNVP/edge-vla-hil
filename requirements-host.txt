# PC host (x86) — simulation plant + ACT policy in PyTorch fallback mode.
# ROS2 Humble itself is installed via apt, not pip.
numpy>=1.24,<2.0
robosuite>=1.4
mujoco>=3.1
torch>=2.2
lerobot @ git+https://github.com/huggingface/lerobot.git
opencv-python>=4.8
onnx>=1.15
onnxruntime>=1.17
pyyaml>=6.0

# PushT sanity rig (throwaway, non-ROS). Install into a separate venv from the ROS workspace.
#
# LOCKED to a self-consistent, known-good baseline. Rationale:
#   * lerobot 0.4.0 (Oct 2025) moved normalization OUT of the policy into a separate
#     PolicyProcessorPipeline, changing how a pretrained checkpoint is loaded AND run.
#   * lerobot 0.3.3 (Aug 2025) is the last release where DiffusionPolicy carries built-in
#     normalization, so from_pretrained + select_action work like the canonical example.
#
# Install the PushT *extra* — it pulls a mutually-consistent gym-pusht + pymunk + gymnasium
# (lerobot 0.3.3 itself pins gymnasium>=0.29.1,<1.0.0, so do NOT hand-pin gymnasium).
lerobot[pusht]==0.3.3
matplotlib>=3.7
pytest>=7
# numpy comes transitively via lerobot; for the pure-numpy core alone, `pip install numpy pytest`.
#
# torch: lerobot 0.3.3 requires torch>=2.2.1,<2.8.0. On Colab the preinstalled CUDA torch is
# usually in range; if pip tries to downgrade it across CUDA versions and GPU breaks, restart the
# runtime, then `pip install "torch==2.7.*"` from the Colab CUDA index before installing lerobot.
